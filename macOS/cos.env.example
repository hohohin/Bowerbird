# 复制为 macOS/.signing/cos.env 并填入真实值；.signing/ 已被 Git 忽略，不要提交真实凭据。
# 获取方式（腾讯云控制台）：
#   SecretId / SecretKey：访问管理 CAM → API 密钥管理；建议子账号并仅授予目标存储桶的 COS 读写权限
#   Bucket：存储桶名（含 appid 后缀，如 bowerbird-downloads-1250000000；不是 CDN 域名）
#   Region：存储桶所在地域，如 ap-guangzhou（与官网服务器同地域，CDN 回源走内网）
#   PUBLIC_BASE：CDN 加速域名基地址；现役默认值已内置 release.sh，换域名时在此覆盖
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
COS_BUCKET=
COS_REGION=ap-guangzhou
# COS_PUBLIC_BASE=https://cdn.bowerbird.cn
