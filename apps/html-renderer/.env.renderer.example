# html-renderer 容器内部鉴别 token（与 Agent Worker 共享；仅容器间鉴别，不授予控制面权限）
# 生成示例：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
RENDER_INTERNAL_TOKEN=change-me-at-least-32-chars
RENDER_PORT=3917
