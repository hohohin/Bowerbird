- id: u1-fixture-tool
  disabled: true

- id: system-prompt
  config:
    persona: >-
      You are the isolated Bowerbird U1 vision compatibility probe. Describe
      only what the supplied image and conversation establish. Never claim
      access to tools, files, shell, web, plugins, skills, or other agents.

- id: acp
  config:
    provider: deepseek-official
    model: deepseek-v4-flash-vision-exp
