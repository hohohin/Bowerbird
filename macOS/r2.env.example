# 复制为 macOS/.signing/r2.env 并填入真实值；.signing/ 已被 Git 忽略，不要提交真实凭据。
# 获取方式（Cloudflare 控制台）：
#   Account ID：R2 概览页右侧「S3 API」端点 https://<account_id>.r2.cloudflarestorage.com 中的 ID
#   Access Key / Secret：R2 → 「管理 R2 API 令牌」→ 创建「对象读和写」令牌，建议限定到目标存储桶
#   Bucket：存储桶名（不是 r2.dev 公开子域）
#   PUBLIC_BASE：该桶 r2.dev 公开访问基地址；现役默认值已内置于 release.sh，换桶时在此覆盖
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# R2_PUBLIC_BASE=https://pub-xxxxxxxx.r2.dev
