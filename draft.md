# Bowerbird 园丁鸟

这是一个通过`搜集图片素材`组成`提示词素材库`后，让用户能通过`点选`想要的`风格/调性/动作/质感`的选项，即可以`相对固定`地批量产出`图片素材`的项目。

> 直觉上，我感觉本项目可以通过 Hermes/openclaw 形式实现。用户通过对话的形式提供素材图片，测试提示词，获取图片，并定期获得新生成的素材图片。

## 用户体验路径

```
# 素材库组建

0.0 用户找到了喜欢的图片
    0.1 拖到 app 里
        0.11 先存着，之后再拆解 -> 1.0
        0.12 马上拆解 -> 1.1

1.0 用户在 raw 界面内选择（可批量）想要入库的图片/素材，点击分析
    1.1 app 对于用户搜集的图片，根据`反推规则`提问用户，根据用户的回答得出图片的反推提示词。
        1.11 给出测试按钮，用户点击后，则根据刚刚得出的反推提示词生成数张图片，让用户判断提示词是否能复现想要的效果
            1.111 不能复现，根据`反推规则`建议用户可以如何修改提问的维度，重新进行反推 -> 1.1
            1.112 能够复现，用户点击`加入到素材库`
                1.1121 程序化建立该图片与提示词的映射，并生成该图片的提示词 intro 用于后续 hover 时的提示
                1.1122 让用户选择将图片加入到哪个库当中
                    1.11221 程序化将图片、提示词存入对应的文件系统内，建立搜索索引

# 素材生成

0.0 - 1 通过web ui 进行生成
        - 选用途
        
    - 2 通过 IU（钉钉/飞书等）通过对话进行调用
```